import { ColorSchemeButton } from './ColorScheme.button';
import { ColorSchemeSwitch } from './ColorScheme.switch';

export { ColorSchemeButton, ColorSchemeSwitch };

export const ColorScheme = {
  Button: ColorSchemeButton,
  Switch: ColorSchemeSwitch,
};

export default ColorScheme;

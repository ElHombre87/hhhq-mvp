import model from './controllers.model';
import machine from './controllers.machine';
import * as selectors from './controllers.selectors'
// export { model, machine };

export default {
  model,
  machine,
  selectors,
}

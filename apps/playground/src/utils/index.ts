import * as filter from './filters';
import * as format from './format';
import * as theme from './theme.utils';

import { getColor } from './theme.utils';
import { createUseContext } from './create-use-context';

export { filter, format, theme, createUseContext, getColor };

export default { filter, format, theme, createUseContext, getColor };
